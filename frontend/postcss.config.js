/**
 * PostCSS Configuration
 * 
 * Configures PostCSS plugins for CSS processing.
 * Includes Tailwind CSS and Autoprefixer for cross-browser compatibility.
 */
export default {
  plugins: {
    tailwindcss: {},      // Process Tailwind CSS directives
    autoprefixer: {},     // Add vendor prefixes for browser compatibility
  },
}
